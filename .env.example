# Copy this file to .env and set your values
# Use a MongoDB connection string (Atlas or local). Example local:
# MONGODB_URL=mongodb://127.0.0.1:27017/meesho123
# Example Atlas (replace <user>,<pass>,<cluster> and <dbname>):
# MONGODB_URL=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# If you don't want to use an env var, leave empty and the project currently
# uses the hardcoded Atlas URI in src/configs/db.js (you can modify that file).
MONGODB_URL=
